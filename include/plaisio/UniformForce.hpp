#ifndef __UNIFORM_FORCE_H
#define __UNIFORM_FORCE_H

namespace plaisio {
    struct UniformForce {
        double x;
        double z;

        double length;
        double angle;

        double magnitude;
    };
}

#endif//__UNIFORM_FORCE_H
